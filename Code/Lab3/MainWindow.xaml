<Window x:Class="Lab3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab3"
        mc:Ignorable="d"
        Title="Lab3" Height="719" Width="832">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        
        <!-- Controllers Panel -->
        <Border Grid.Row="0" Background="LightGray">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Input Fields -->
                <StackPanel Grid.Column="0">
                    <Label Content="Введите p" FontSize="14" Margin="0,5"/>
                    <TextBox x:Name="TbInputP" FontSize="14" Margin="0,0,0,10" TextChanged="tb_input_TextChanged" PreviewTextInput="tb_input_PreviewTextInput" PreviewKeyDown="tb_input_PreviewKeyDown"/>
                    
                    <Label Content="Введите q" FontSize="14" Margin="0,5"/>
                    <TextBox x:Name="TbInputQ" FontSize="14" Margin="0,0,0,10" TextChanged="tb_input_TextChanged" PreviewTextInput="tb_input_PreviewTextInput" PreviewKeyDown="tb_input_PreviewKeyDown"/>
                    
                    <Label Content='Введите b' FontSize="14" Margin="0,5"/>
                    <TextBox x:Name="TbInputB" FontSize="14" Margin="0,0,0,10" TextChanged="tb_input_TextChanged" PreviewTextInput="tb_input_PreviewTextInput" PreviewKeyDown="tb_input_PreviewKeyDown"/>
                </StackPanel>
                
                <!-- Radio Buttons and Execute -->
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <RadioButton x:Name="RbCrypto" Content="Шифровать" FontSize="14" Margin="0,10" IsChecked="True"/>
                    <RadioButton x:Name="RbDecrypt" Content="Дешифрировать" FontSize="14" Margin="0,10" />
                    <Button x:Name="BtnExecute" IsEnabled="False" Content="Выполнить" FontSize="14" Width="165" Height="31" Margin="0,20" Click="btn_execute_Click"/>
                </StackPanel>
                
                <!-- File Operations -->
                <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                    <Button x:Name="BtnOpenSimpleFile" Content="Открыть обычный файл" FontSize="14" Width="225" Height="31" Margin="0,10" Click="BtnOpenSimpleFile_OnClick"/>
                    <Button x:Name="BtnOpenEncryptedFile" Content="Открыть зашифрованный файл" FontSize="14" Width="225" Height="31" Margin="0,10" Click="BtnOpenEncryptedFile_OnClick"/>
                    <Button x:Name="BtnSaveFile" Content="Сохранить обычный файл" FontSize="14" Width="225" Height="31" Margin="0,10" Click="BtnSaveFile_OnClick"/>
                    <Button x:Name="BtnSaveEncryptedFile" Content="Сохранить зашифрованный файл" FontSize="14" Width="225" Height="31" Margin="0,10" Click="BtnSaveEncryptedFile_OnClick"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Data Panel -->
        <Grid Grid.Row="1" Background="DimGray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Source Data -->
            <Border Grid.Column="0" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Label Content="Исходные данные" FontSize="14" HorizontalAlignment="Center" Grid.Row="0"/>
                    <TextBox x:Name="TbSource" IsReadOnly="True" Margin="5" Grid.Row="1" AcceptsReturn="True" 
                             TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                             TextChanged="tb_input_TextChanged"/>
                </Grid>
            </Border>
            
            <!-- Result Data -->
            <Border Grid.Column="1" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Label Content="Результат" FontSize="14" HorizontalAlignment="Center" Grid.Row="0"/>
                    <TextBox x:Name="TbResult" IsReadOnly="True" Margin="5" Grid.Row="1" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>